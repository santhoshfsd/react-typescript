import axios from 'axios';
import { ActionType } from '../action-types';
import { Action } from '../action';

//creator manaully dispatch actions

const search = (term: string) => {
    return async (dispatch: any) => {

    };
}